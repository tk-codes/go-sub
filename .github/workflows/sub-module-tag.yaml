name: Create Submodule

on:
  push:
    branches:
      - main
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: write  # Grant write permissions to allow creating and pushing tags

jobs:
  check-changes:
    runs-on: ubuntu-latest
    outputs:
      folder_changed: ${{ steps.check_changes.outputs.folder_changed }}
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v3

      - name: Get the previous tag
        id: get_prev_tag
        run: |
          git tag --list 'v*' --sort=-creatordate
          # Get the previous tag before the current one
          PREV_TAG=$(git tag --list 'v*' --sort=-creatordate | tail -n +2 | head -n 1)
          echo "Previous tag is $PREV_TAG"
          echo "::set-output name=prev_tag::$PREV_TAG"

      - name: Check for changes in the folder
        id: check_changes
        run: |
          # Compare changes between the previous tag and the current tag in the target folder
          if git diff --name-only ${{ steps.get_prev_tag.outputs.prev_tag }} HEAD -- pkg/math | grep -q .; then
            echo "Changes detected in the submodule"
            echo "::set-output name=folder_changed::true"
          else
            echo "No changes in the submodule"
            echo "::set-output name=folder_changed::false"
          fi  

  create-tag:
    name: Create submodule package tag
    needs: check-changes
    runs-on: ubuntu-latest
    if: needs.check-changes.outputs.folder_changed == 'true'

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v3

      - name: Set up Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Extract Tag from Release
        id: extract_tag
        run: |
          tag_name="${{ github.ref_name }}"
          echo "tag_name=$tag_name" >> $GITHUB_ENV

      - name: Create and Push New Tag
        run: |
          new_tag="pkg/math/${{ env.tag_name }}"
          git tag "$new_tag"
          git push origin "$new_tag"
